<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.holygunner.discover_meals">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.holygunner.discover_meals.App"
        android:allowBackup="true"
        tools:replace="android:allowBackup"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiscoverMeals2020">
        <activity
            android:name="com.holygunner.discover_meals.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.DiscoverMeals2020.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.work.impl.WorkManagerInitializer"
            android:authorities="com.holygunner.discover_meals.workmanager-init"
            tools:node="remove"/>

    </application>

</manifest>